# kfs

* kernel frpom scratch


## todo 

Phase 9: Next Steps / Expansion

Add basic functions: strlen, strcmp, etc.

Add types: uint8_t, size_t

Build a Makefile rule for multiple C/ASM files

Experiment with screen colors, cursor, keyboard input

➜  kfs-1 git:(main) ✗ qemu-system-i386 -boot d -cdrom "/Users/arevikmkrtchyan/Desktop/kfs/kfs-1/mykernel.iso" -m 512M -serial stdio -display none
42